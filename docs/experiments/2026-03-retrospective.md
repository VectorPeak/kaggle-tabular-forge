# March Experiment Retrospective

Retrospective notes for tabular experiments:

- The validation split should be treated as a project artifact, not a temporary setting.
- Feature candidates need rejection reasons when they are removed.
- Out-of-fold predictions are the base layer for later ensembling work.
- Leakage checks should happen before model complexity increases.

The next step is to keep experiment outputs compact enough for regular review.
